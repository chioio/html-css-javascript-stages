/* -------------------------------------------------------------------------- */
/*                                    UTILS                                   */
/* -------------------------------------------------------------------------- */
const handleWrapperOverflow = (el) => {
  carouselEl.style.overflow = el.checked ? 'hidden' : 'visible';
};
